(function(global){'use strict';
const NS=global.InkDOS2Spreadsheets=global.InkDOS2Spreadsheets||{};
let deliveryInFlight=null;
function singleFlight(run){if(deliveryInFlight)return deliveryInFlight;let p;try{p=Promise.resolve(run())}catch(e){return Promise.reject(e)}deliveryInFlight=p;p.finally(()=>{if(deliveryInFlight===p)deliveryInFlight=null}).catch(()=>{});return p}
function fail(code,message,cause){const e=new Error(message);e.name='InkDOSSpreadsheetsDeliveryError';e.code=code;if(cause)e.cause=cause;throw e}
function safeName(name,sourceKind='xlsx'){let n=String(name||'Untitled.xlsx').trim()||'Untitled.xlsx';n=n.replace(/[\\/:*?"<>|]+/g,'-').replace(/\.(xls|xlsx|csv|tsv)$/i,'');const ext=sourceKind==='csv'?'.csv':sourceKind==='tsv'?'.tsv':'.xlsx';return(n||'Untitled')+ext}
function mimeFor(sourceKind){return sourceKind==='csv'?'text/csv;charset=utf-8':sourceKind==='tsv'?'text/tab-separated-values;charset=utf-8':'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'}
function extensionFor(sourceKind){return sourceKind==='csv'?'.csv':sourceKind==='tsv'?'.tsv':'.xlsx'}
function toFile(blob,fileName,sourceKind='xlsx'){try{return new File([blob],safeName(fileName,sourceKind),{type:mimeFor(sourceKind),lastModified:Date.now()})}catch(_){return null}}
function canShare(file){if(!file||typeof navigator==='undefined'||typeof navigator.share!=='function'||typeof navigator.canShare!=='function')return false;try{return !!navigator.canShare({files:[file]})}catch(_){return false}}
function isAppleTouchHost(){const nav=global.navigator||{};const ua=String(nav.userAgent||'');const platform=String(nav.platform||'');const touches=Number(nav.maxTouchPoints||0);return /iPad|iPhone|iPod/i.test(ua)||(platform==='MacIntel'&&touches>1)}
function capabilities(blob,fileName,{sourceKind='xlsx'}={}){const file=toFile(blob,fileName,sourceKind);return Object.freeze({fileSystem:typeof global.showSaveFilePicker==='function',share:canShare(file),download:!!(global.document&&global.URL&&URL.createObjectURL),local:!!(global.location&&global.location.protocol==='file:'),preferShareSave:isAppleTouchHost()})}
async function viaPicker(blob,fileName,sourceKind='xlsx'){let handle;try{handle=await global.showSaveFilePicker({suggestedName:safeName(fileName,sourceKind),types:[{description:sourceKind==='csv'?'CSV file':sourceKind==='tsv'?'TSV file':'Excel workbook',accept:{[mimeFor(sourceKind)]:[extensionFor(sourceKind)]}}]})}catch(e){if(e?.name==='AbortError')fail('cancelled','Save cancelled.',e);fail('picker-blocked','Native save picker was blocked by this host.',e)}try{const writable=await handle.createWritable();await writable.write(blob);await writable.close()}catch(e){fail('write-failed',`The selected ${sourceKind==='csv'?'CSV':sourceKind==='tsv'?'TSV':'XLSX'} could not be written.`,e)}return Object.freeze({method:'file-system-access',fileName:safeName(fileName,sourceKind),deliveryConfirmed:true})}
async function viaShare(blob,fileName,file,sourceKind='xlsx'){try{await navigator.share({files:[file]})}catch(e){if(e?.name==='AbortError')fail('cancelled','Share cancelled.',e);fail('share-blocked','System file sharing was blocked by this host.',e)}return Object.freeze({method:'web-share',fileName:safeName(fileName,sourceKind),deliveryConfirmed:false})}
async function share(blob,fileName,{sourceKind='xlsx'}={}){const file=toFile(blob,fileName,sourceKind);if(!canShare(file))fail('share-unavailable','System file sharing is unavailable in this host.');return viaShare(blob,fileName,file,sourceKind)}
async function viaDownload(blob,fileName,sourceKind='xlsx'){if(!(global.document&&global.URL&&URL.createObjectURL))fail('download-unavailable','Download is unavailable in this host.');const u=URL.createObjectURL(blob),a=document.createElement('a');a.href=u;a.download=safeName(fileName,sourceKind);a.rel='noopener';a.hidden=true;document.body.appendChild(a);try{a.click()}finally{a.remove();setTimeout(()=>URL.revokeObjectURL(u),15000)}return Object.freeze({method:'download',fileName:safeName(fileName,sourceKind),deliveryConfirmed:false})}
async function deliverOnce(blob,fileName,{sourceKind='xlsx'}={}){const file=toFile(blob,fileName,sourceKind),c=capabilities(blob,fileName,{sourceKind});
 // Any invoked delivery route is terminal for this Save action. A rejected Web Share promise does not prove that the host produced no file.
 if(c.local){if(c.share)return viaShare(blob,fileName,file,sourceKind);fail('local-delivery-unavailable',`This HTML viewer blocks direct ${sourceKind==='csv'?'CSV':sourceKind==='tsv'?'TSV':'XLSX'} downloads. Use a browser/host with system file sharing enabled, or open the InkDOS app from its normal host.`);}
 if(c.preferShareSave&&c.share)return viaShare(blob,fileName,file,sourceKind)
 if(c.fileSystem){try{return await viaPicker(blob,fileName,sourceKind)}catch(e){if(e.code==='cancelled'||e.code==='write-failed')throw e;if(c.share)return viaShare(blob,fileName,file,sourceKind);return viaDownload(blob,fileName,sourceKind)}}
 if(c.share)return viaShare(blob,fileName,file,sourceKind)
 return viaDownload(blob,fileName,sourceKind)
}
function deliver(blob,fileName,options){return singleFlight(()=>deliverOnce(blob,fileName,options))}
NS.FileDelivery=Object.freeze({deliver,share,capabilities,safeName});
})(globalThis);