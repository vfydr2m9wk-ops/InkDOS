(function(root){'use strict';
const NS=root.InkDOS2Spreadsheets=root.InkDOS2Spreadsheets||{};let colorFidelityLoader=null;
function readFile(file){if(file.arrayBuffer)return file.arrayBuffer();return new Promise((resolve,reject)=>{const r=new FileReader();r.onerror=()=>reject(r.error||new Error('File read failed'));r.onload=()=>resolve(r.result);r.readAsArrayBuffer(file)})}
function isLegacy(file,buffer){if(/\.xls$/i.test(file?.name||'')&&!/\.xlsx$/i.test(file?.name||''))return true;const b=new Uint8Array(buffer,0,Math.min(8,buffer.byteLength));const sig=[0xD0,0xCF,0x11,0xE0,0xA1,0xB1,0x1A,0xE1];return b.length===8&&sig.every((v,i)=>b[i]===v)}
async function digest(bytes){try{const h=await crypto.subtle.digest('SHA-256',bytes);return[...new Uint8Array(h)].map(b=>b.toString(16).padStart(2,'0')).join('')}catch(_){let x=2166136261;for(const b of new Uint8Array(bytes)){x^=b;x=Math.imul(x,16777619)>>>0}return'fallback-'+x.toString(16)}}
function ensureColorFidelity(){if(root.InkDOS2SpreadsheetColorFidelity)return Promise.resolve();if(colorFidelityLoader)return colorFidelityLoader;colorFidelityLoader=new Promise((resolve,reject)=>{const s=document.createElement('script');s.src='io/xlsx-color-fidelity.js';s.onload=()=>root.InkDOS2SpreadsheetColorFidelity?resolve():reject(new Error('XLSX color fidelity module did not initialize'));s.onerror=()=>reject(new Error('Could not load XLSX color fidelity module'));document.head.appendChild(s)});return colorFidelityLoader}
function create({session,fileInput,dialog,setLoading,onCommitted,onError}={}){let lastToken='';
 async function authorize(kind){if(!session.dirty)return true;return dialog.confirm({title:'Unsaved changes',message:kind==='open'?'Discard current unsaved changes and open another workbook?':'Discard current unsaved changes and create a new workbook?',confirmLabel:kind==='open'?'Open workbook':'New workbook'})}
 async function requestOpen(){if(!await authorize('open'))return false;fileInput.value='';fileInput.click();return true}
 async function handle(file){if(!file)return{status:'cancelled'};const token=[session.documentId,file.name,file.size,file.lastModified||0].join(':');if(token===lastToken)return{status:'duplicate'};lastToken=token;const op=session.beginOperation();try{setLoading?.(true,'Opening workbook…');if(!/\.(xls|xlsx)$/i.test(file.name||''))throw new Error('Choose an Excel XLS or XLSX workbook.');NS.PackageValidator.validateInputSize(file.size,file.name);const buffer=await readFile(file),legacy=isLegacy(file,buffer);if(!legacy)await ensureColorFidelity();const book=legacy?await root.LocalXLS.parseWorkbook(buffer,file.name):await root.LocalXLSX.parseWorkbook(buffer,file.name);if(!session.isCurrent(op))return{status:'stale'};const hash=await digest(buffer);if(!session.commitCandidate({book,fileName:file.name,documentId:(legacy?'xls-':'xlsx-')+hash.slice(0,16),sourceKind:legacy?'xls':'xlsx'},op))return{status:'stale'};onCommitted?.({book,legacy,file,hash});return{status:'committed',legacy,book}}catch(error){lastToken='';onError?.(error);return{status:'rejected',error}}finally{setLoading?.(false);fileInput.value=''}}
 fileInput.addEventListener('change',()=>handle(fileInput.files?.[0]));fileInput.addEventListener('input',()=>handle(fileInput.files?.[0]));
 async function newWorkbook(){if(!await authorize('new'))return false;const book=root.LocalXLSX.createBlank();book.loaded=true;book.fileName='Untitled.xlsx';session.newBook(book);lastToken='';onCommitted?.({book,legacy:false,file:null,newBook:true});return true}
 return Object.freeze({requestOpen,handle,newWorkbook,authorize})}
NS.FileOpenController=Object.freeze({create});
})(globalThis);
