(function(global){'use strict';
const NS=global.InkDOS2PdfP4=global.InkDOS2PdfP4||{},$=id=>document.getElementById(id);
function create({session,chrome,fileOpen,save,layout,editor,navigation,modeController,readerTools,pageTools,registry,extensions}={}){const drawer=NS.FrameUI.createDrawer({trigger:$('menuBtn'),drawer:$('generalMenu'),backdrop:$('menuBackdrop'),closeButton:$('closeMenuBtn')});const bindings=NS.CommandBindings.create({registry,navigation,session,drawer});let unsavedDialog=null;
 function closeDrawer(){if(drawer.isOpen)drawer.close();registry.sync('frame.menu.close')}
 function syncPage(){const n=layout.currentPage||1;$('pageInput').value=String(n);$('pageCount').textContent='/ '+(layout.pageCount||0);chrome.page(n,layout.pageCount);navigation?.syncCurrent(n);editor.setCurrentPage(n);readerTools?.syncEnabled();pageTools?.syncEnabled();registry.sync()}
 function syncEditor(){registry.sync('history.undo');registry.sync('history.redo');registry.sync('annotation.delete')}
 function hasPendingCommentDraft(){if(!extensions?.commentIsOpen?.())return false;const ta=$('commentText');if(!ta)return false;const current=String(ta.value||''),id=extensions.commentEditingId,baseline=id?String(extensions.items?.get?.(id)?.note||''):'';return current!==baseline}
 function hasUnsavedWork(){editor.commit();return !!(session.dirty||hasPendingCommentDraft())}
 async function commitPendingCommentDraft(){if(!hasPendingCommentDraft())return true;return !!(await extensions.saveComment())}
 function discardPendingCommentDraft(){if(!extensions?.commentIsOpen?.())return;extensions.closeComment();extensions.clearSelection?.()}
 function closeUnsavedDialog(choice){if(!unsavedDialog)return;const {node,resolve}=unsavedDialog;unsavedDialog=null;node.remove();resolve(choice)}
 function decideUnsaved(message){
  if(!hasUnsavedWork())return Promise.resolve('discard');if(unsavedDialog)return Promise.resolve('cancel');
  return new Promise(resolve=>{const node=document.createElement('div');node.id='pdfUnsavedDialog';node.setAttribute('role','dialog');node.setAttribute('aria-modal','true');node.setAttribute('aria-label','Unsaved changes');node.style.cssText='position:fixed;inset:0;z-index:1000;display:grid;place-items:center;padding:20px;background:rgba(0,0,0,.42)';const card=document.createElement('div');card.style.cssText='width:min(420px,100%);border:1px solid rgba(127,127,127,.35);border-radius:14px;padding:18px;background:Canvas;color:CanvasText;box-shadow:0 20px 60px rgba(0,0,0,.3);font:14px/1.45 system-ui,-apple-system,sans-serif';const title=document.createElement('h2');title.textContent='Unsaved changes';title.style.margin='0 0 8px';const text=document.createElement('p');text.textContent=String(message||'Save your PDF edits before continuing?');text.style.margin='0 0 16px';const actions=document.createElement('div');actions.style.cssText='display:flex;justify-content:flex-end;gap:8px;flex-wrap:wrap';for(const [choice,label] of [['cancel','Cancel'],['discard','Discard'],['save','Save']]){const button=document.createElement('button');button.type='button';button.dataset.choice=choice;button.textContent=label;button.style.cssText='min-height:38px;padding:7px 12px;border-radius:9px;border:1px solid rgba(127,127,127,.45);background:ButtonFace;color:ButtonText';button.onclick=()=>closeUnsavedDialog(choice);actions.append(button)}card.append(title,text,actions);node.append(card);unsavedDialog={node,resolve};document.body.append(node);actions.querySelector('[data-choice="cancel"]')?.focus()})
 }
 async function authorizeReplacement(kind){
  if(!hasUnsavedWork())return true;
  const messages={open:'Save your current PDF edits before opening another PDF?',leave:'Save your current PDF edits before leaving PDF?'};
  const decision=await decideUnsaved(messages[kind]||messages.leave);
  if(decision==='discard'){discardPendingCommentDraft();return true}if(decision==='cancel')return false;if(decision==='save'){if(!(await commitPendingCommentDraft()))return false;return await save.saveForReplacement()}return false
 }
 async function openPdf(){if(!(await authorizeReplacement('open')))return false;closeDrawer();return fileOpen.chooseFile()}
 function registerCommands(){registry.register('file.open',{execute:openPdf});registry.register('file.save',{isEnabled:()=>!!session.active,execute:async()=>{closeDrawer();if(!(await commitPendingCommentDraft()))return null;return save.save()}});registry.register('file.share',{isEnabled:()=>!!session.active,execute:()=>{closeDrawer();return save.share()}});registry.register('navigation.previous',{isEnabled:()=>!!session.active&&layout.currentPage>1,execute:()=>layout.prev()});registry.register('navigation.next',{isEnabled:()=>!!session.active&&layout.currentPage<(layout.pageCount||0),execute:()=>layout.next()});registry.register('history.undo',{isEnabled:()=>!!editor.inspect().state.hasSomethingToUndo,execute:()=>{editor.undo();setTimeout(syncEditor,0)}});registry.register('history.redo',{isEnabled:()=>!!editor.inspect().state.hasSomethingToRedo,execute:()=>{editor.redo();setTimeout(syncEditor,0)}});registry.register('annotation.delete',{isEnabled:()=>!!editor.inspect().state.hasSelectedEditor,execute:()=>{editor.deleteSelected();setTimeout(syncEditor,0)}})}
 function install(){registerCommands();bindings.install();const home=document.querySelector('a[aria-label="Home"]');if(home)home.addEventListener('click',async event=>{if(!hasUnsavedWork())return;event.preventDefault();event.stopPropagation();const href=home.href;if(await authorizeReplacement('leave'))global.location.assign(href)});$('pageInput').addEventListener('change',()=>layout.goToPage(Number($('pageInput').value)));$('textSize').onchange=e=>editor.setTextSize(e.target.value);$('textColor').onchange=e=>editor.setTextColor(e.target.value);$('penColor').onchange=e=>editor.setPenColor(e.target.value);$('penThickness').onchange=e=>editor.setPenThickness(e.target.value);$('penOpacity').onchange=e=>editor.setPenOpacity(e.target.value);document.addEventListener('keydown',e=>{const key=e.key.toLowerCase(),editing=/^(INPUT|TEXTAREA|SELECT)$/.test(document.activeElement?.tagName||'')||document.activeElement?.isContentEditable,mod=e.metaKey||e.ctrlKey;if(mod&&key==='o'){e.preventDefault();registry.execute('file.open');return}if(mod&&key==='s'){e.preventDefault();registry.execute('file.save');return}if(mod&&key==='f'){e.preventDefault();registry.execute('reader.search');return}if(mod&&key==='p'){e.preventDefault();registry.execute('reader.print');return}if(!editing&&mod&&key==='z'&&modeController.mode==='annotate'){e.preventDefault();registry.execute(e.shiftKey?'history.redo':'history.undo');return}if(!editing&&e.key==='Delete'&&modeController.mode==='annotate'){registry.execute('annotation.delete');return}if(!editing&&!mod&&key==='r'&&modeController.mode==='view'){e.preventDefault();registry.execute('reader.rotate-view');return}if(!editing&&e.key==='Escape'){registry.execute('reader.search.close');registry.execute('pdf.pages.panel.close');registry.execute('pdf.navigation.close');registry.execute('frame.menu.close');editor.unselect();modeController.setTool('none');syncEditor()}});syncEditor();syncPage()}
 return Object.freeze({install,syncEditor,syncPage,registry,bindings,drawer,authorizeReplacement,decideUnsaved,hasUnsavedWork,hasPendingCommentDraft,commitPendingCommentDraft,discardPendingCommentDraft,inspect:()=>Object.freeze({commands:registry.inspect(),bindings:bindings.inspect(),drawerOpen:drawer.isOpen})})
}
NS.CommandController=Object.freeze({create});})(globalThis);
