(function(global){'use strict';const NS=global.InkDOS2PdfP4=global.InkDOS2PdfP4||{};
function create({layout,chrome,anchor}={}){const $=id=>document.getElementById(id);let doc=null,pageCount=0,currentPage=1,thumbBlock=0,outline=[],currentTab='outline';const blockSize=24,DEFAULT_TAB='outline',resize=()=>{if(!$('navigationPanel').hidden)position()};
 async function setDocument(pdf,count){doc=pdf;pageCount=count||0;currentPage=1;thumbBlock=0;try{outline=await doc.getOutline()||[]}catch(_){outline=[]}renderOutline();if(!$('navigationPanel').hidden&&currentTab==='pages')renderThumbs();syncCurrent()}
 function flatten(items,depth=0,out=[]){for(const it of items||[]){out.push({it,depth});if(it.items?.length)flatten(it.items,depth+1,out)}return out}
 async function destinationPage(it){try{let dest=it.dest;if(typeof dest==='string')dest=await doc.getDestination(dest);if(!Array.isArray(dest)||!dest[0])return null;return(await doc.getPageIndex(dest[0]))+1}catch(_){return null}}
 function renderOutline(){const host=$('outlineList');host.replaceChildren();const flat=flatten(outline);if(!flat.length){host.innerHTML='<div class="nav-empty">No document outline</div>';return}for(const{it,depth}of flat){const b=document.createElement('button');b.type='button';b.className='outline-item';b.style.setProperty('--depth',String(Math.min(depth,6)));b.textContent=it.title||'Untitled';b.onclick=async()=>{const n=await destinationPage(it);if(n){close();layout.goToPage(n)}};host.append(b)}}
 async function renderThumbs(){const grid=$('thumbGrid');grid.replaceChildren();if(!doc||!pageCount)return;const start=thumbBlock*blockSize+1,end=Math.min(pageCount,start+blockSize-1);$('thumbRange').textContent=`${start}–${end} / ${pageCount}`;$('thumbPrevBlock').disabled=start<=1;$('thumbNextBlock').disabled=end>=pageCount;for(let n=start;n<=end;n++){const card=document.createElement('button');card.type='button';card.className='thumb-card'+(n===currentPage?' current':'');card.dataset.page=String(n);const canvas=document.createElement('canvas'),label=document.createElement('span');label.textContent=String(n);card.append(canvas,label);card.onclick=()=>{close();layout.goToPage(n)};grid.append(card);renderThumb(n,canvas).catch(()=>{})}}
 async function renderThumb(n,canvas){const page=await doc.getPage(n),base=page.getViewport({scale:1}),scale=Math.min(130/base.width,170/base.height),vp=page.getViewport({scale});canvas.width=Math.max(1,Math.round(vp.width));canvas.height=Math.max(1,Math.round(vp.height));const ctx=canvas.getContext('2d',{alpha:false});ctx.fillStyle='#fff';ctx.fillRect(0,0,canvas.width,canvas.height);await page.render({canvasContext:ctx,viewport:vp,background:'#fff'}).promise}
 function open(tab=DEFAULT_TAB){$('navigationPanel').hidden=false;setTab(tab);position();return true}
 function close(){$('navigationPanel').hidden=true;return true}
 function toggle(){return $('navigationPanel').hidden?open(DEFAULT_TAB):close()}
 function position(){const p=$('navigationPanel'),connected=anchor?.isConnected?anchor:null,a=connected?.getBoundingClientRect();if(!a){p.style.left='8px';p.style.top='56px';return}p.style.left=Math.max(8,Math.min(innerWidth-p.offsetWidth-8,a.left))+'px';p.style.top=Math.max(8,Math.min(innerHeight-p.offsetHeight-8,a.bottom+6))+'px'}
 function setTab(tab){currentTab=tab==='pages'?'pages':'outline';$('outlineTab').classList.toggle('active',currentTab==='outline');$('pagesTab').classList.toggle('active',currentTab==='pages');$('outlinePane').classList.toggle('active',currentTab==='outline');$('pagesPane').classList.toggle('active',currentTab==='pages');if(currentTab==='pages')renderThumbs();return currentTab}
 function syncCurrent(n=layout.currentPage){currentPage=n||1;for(const c of document.querySelectorAll('#thumbGrid .thumb-card'))c.classList.toggle('current',Number(c.dataset.page)===currentPage)}
 function install(){setTab(DEFAULT_TAB);$('closeNavigation').onclick=close;$('outlineTab').onclick=()=>setTab('outline');$('pagesTab').onclick=()=>setTab('pages');$('thumbPrevBlock').onclick=()=>{thumbBlock=Math.max(0,thumbBlock-1);renderThumbs()};$('thumbNextBlock').onclick=()=>{thumbBlock=Math.min(Math.floor((pageCount-1)/blockSize),thumbBlock+1);renderThumbs()};global.addEventListener('resize',resize,{passive:true})}
 function destroy(){global.removeEventListener('resize',resize);close();doc=null;outline=[]}
 return Object.freeze({install,destroy,setDocument,syncCurrent,open,close,toggle,setTab,inspect:()=>Object.freeze({pageCount,currentPage,outlineItems:flatten(outline).length,thumbBlock,tab:currentTab})})}
NS.NavigationController=Object.freeze({create,DEFAULT_TAB:'outline'});})(globalThis);
