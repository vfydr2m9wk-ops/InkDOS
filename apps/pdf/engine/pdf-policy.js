(function(global){'use strict';const NS=global.InkDOS2PdfP4=global.InkDOS2PdfP4||{};
function positive(v,fallback=1){v=Number(v);return Number.isFinite(v)&&v>0?v:fallback}
function normalizeRotation(v){v=((Math.trunc(Number(v)||0)%360)+360)%360;return v===90||v===180||v===270?v:0}
function effectiveRotation(g,view){return normalizeRotation((g?.rotation||0)+(view?.rotation||0))}
function rotatedSize(g,view){const rotation=effectiveRotation(g,view),swap=rotation===90||rotation===270;return {width:swap?g.height:g.width,height:swap?g.width:g.height,rotation}}
function computePolicy(metrics,geometry,view){const size=rotatedSize(geometry,view),mode=view?.mode||'fit-width';if(!metrics||!metrics.visible||metrics.availableWidth<=0||metrics.availableHeight<=0)return Object.freeze({suspended:true,mode,scale:0,cssWidth:0,cssHeight:0,rotation:size.rotation});let scale;if(mode==='fit-width')scale=metrics.availableWidth/size.width;else if(mode==='fit-page')scale=Math.min(metrics.availableWidth/size.width,metrics.availableHeight/size.height);else if(mode==='manual')scale=positive(view.manualScale,1);else throw new Error('PDF_VIEW_MODE_UNSUPPORTED');return Object.freeze({suspended:false,mode,scale,cssWidth:size.width*scale,cssHeight:size.height*scale,intrinsicWidth:size.width,intrinsicHeight:size.height,rotation:size.rotation})}
function rasterBudget(cssWidth,cssHeight,dpr,maxPixels=8*1024*1024){const requested=positive(dpr,1),cap=Math.sqrt(maxPixels/(positive(cssWidth)*positive(cssHeight))),scale=Math.min(requested,cap);return Object.freeze({scale,pixelWidth:Math.max(1,Math.floor(cssWidth*scale)),pixelHeight:Math.max(1,Math.floor(cssHeight*scale))})}
NS.PdfPolicy=Object.freeze({computePolicy,rasterBudget,rotatedSize,effectiveRotation,normalizeRotation});})(globalThis);
