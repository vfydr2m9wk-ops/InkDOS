(function(global){'use strict';const NS=global.InkDOS2PdfP4=global.InkDOS2PdfP4||{};
function positive(v,fallback=1){v=Number(v);return Number.isFinite(v)&&v>0?v:fallback}function rotatedSize(g){const swap=g.rotation===90||g.rotation===270;return {width:swap?g.height:g.width,height:swap?g.width:g.height}}
function computePolicy(metrics,geometry,view){const size=rotatedSize(geometry),mode=view?.mode||'fit-width';if(!metrics||!metrics.visible||metrics.availableWidth<=0||metrics.availableHeight<=0)return Object.freeze({suspended:true,mode,scale:0,cssWidth:0,cssHeight:0});let scale;if(mode==='fit-width')scale=metrics.availableWidth/size.width;else if(mode==='fit-page')scale=Math.min(metrics.availableWidth/size.width,metrics.availableHeight/size.height);else if(mode==='manual')scale=positive(view.manualScale,1);else throw new Error('PDF_VIEW_MODE_UNSUPPORTED');return Object.freeze({suspended:false,mode,scale,cssWidth:size.width*scale,cssHeight:size.height*scale,intrinsicWidth:size.width,intrinsicHeight:size.height})}
function rasterBudget(cssWidth,cssHeight,dpr,maxPixels=8*1024*1024){const requested=positive(dpr,1),cap=Math.sqrt(maxPixels/(positive(cssWidth)*positive(cssHeight))),scale=Math.min(requested,cap);return Object.freeze({scale,pixelWidth:Math.max(1,Math.floor(cssWidth*scale)),pixelHeight:Math.max(1,Math.floor(cssHeight*scale))})}
NS.PdfPolicy=Object.freeze({computePolicy,rasterBudget,rotatedSize});})(globalThis);
