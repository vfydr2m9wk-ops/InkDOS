name: PPTX thumbnail pipeline diagnostic

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  webkit-thumbnail-diagnostic:
    if: github.event.pull_request.head.ref == 'perf/pptx-thumbnail-diagnostic'
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: requirements-ci.txt
      - name: Install validation dependencies
        run: python -m pip install --disable-pip-version-check -r requirements-ci.txt
      - name: Install WebKit
        run: python -m playwright install --with-deps webkit
      - name: Measure slide-to-thumbnail pipeline
        env:
          BROWSER: webkit
          INKDOS_PPTX_THUMB_DIAG_OUT: artifacts/pptx-thumbnail-diagnostic/webkit
        run: python scripts/diagnose_pptx_thumbnail_pipeline.py
      - name: Preserve PPTX thumbnail diagnostic
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pptx-thumbnail-diagnostic-webkit
          path: artifacts/pptx-thumbnail-diagnostic/webkit/
          if-no-files-found: warn
          retention-days: 14
