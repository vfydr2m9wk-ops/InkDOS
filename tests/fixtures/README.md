# Synthetic OOXML Fixtures

These minimal files are generated by `scripts/generate_fixtures.py` and are intended only for regression plumbing:

- `minimal.docx`
- `minimal.xlsx`
- `minimal.pptx`

They do not represent broad format compatibility. More fixtures should be small, redistributable and tied to a specific bug or workflow.
