{
  "schemaVersion": 1,
  "version": "0.20.0",
  "description": "Stable shared interface regions exposed by the InkDOS application shell.",
  "regions": {
    "titlebar": {
      "required": true,
      "purpose": "File identity and global commands",
      "dataAttribute": "data-inkdos-shell-region=titlebar"
    },
    "command-tabs": {
      "required": false,
      "purpose": "Workspace command categories",
      "dataAttribute": "data-inkdos-shell-region=command-tabs"
    },
    "commandbar": {
      "required": false,
      "multiple": true,
      "purpose": "Commands for the active category",
      "dataAttribute": "data-inkdos-shell-region=commandbar"
    },
    "workspace": {
      "required": true,
      "multiple": true,
      "purpose": "Primary document content",
      "dataAttribute": "data-inkdos-shell-region=workspace"
    },
    "statusbar": {
      "required": true,
      "purpose": "Selection, progress, navigation, and zoom status",
      "dataAttribute": "data-inkdos-shell-region=statusbar"
    }
  },
  "panelSides": [
    "left",
    "right",
    "bottom"
  ],
  "events": [
    "inkdos:shell-ready",
    "inkdos:panel-change",
    "inkdos:command-registered",
    "inkdos:command-executed",
    "inkdos:status-change",
    "inkdos:file-state-change"
  ]
}
